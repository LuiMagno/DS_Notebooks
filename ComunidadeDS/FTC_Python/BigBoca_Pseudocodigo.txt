var
    dia, desconto_base, previsao_clientes, desconto_final
inicio
    leia dia
    desconto_base <- 10%

    
    desconto_base <- desconto_base * dia
    se previsao_clientes > 1000 * dia
        desconto_final = ((previsao_clientes - 1000 * Dia)/100 /100) + desconto_base
    senao
        desconto_final = desconto_base
    
    escreva "O desconto final é {desconto_final}"
            